name = "elevate-cms-auth"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets (do NOT commit these — set them with wrangler):
#   wrangler secret put GITHUB_CLIENT_ID
#   wrangler secret put GITHUB_CLIENT_SECRET
